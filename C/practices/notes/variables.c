// This is a comment in C!
#include <stdio.h> // Lets us use inputs and outputs

int main(){
    //variables examples
    int age = 9999;
    float gpa = 3.98;
    char grade = 'A'; // single letter uses sinle quotes
    char name[] = "Eric";

    printf("It worked!\n");
    printf("%d", age);
    printf("Your GPA is: %f\n", gpa);
    printf("You have a %c in computer scinece\n", grade);
    return 0; // required last line
}